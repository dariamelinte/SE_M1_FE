import React from 'react';

type RegisteredAreasProps = {};

const RegisteredAreas: React.FC<RegisteredAreasProps> = () => (
  <div className="relative flex justify-center">Registered Areas</div>
);

export default RegisteredAreas;
